version: 2.1
jobs:
  build:
    machine:
      image: ubuntu-2204:current
      docker_layer_caching: true
    resource_class: large
    steps:
      - checkout
      - run: apt list --installed
      - run: |
          docker build --platform -t codeberg.org/sa/mstd-random-restaurant:lates





































--**-Mg: config.yml                       (fundamental-c)--L7--C33-------------------------------------------------
