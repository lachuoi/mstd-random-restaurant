version: 2.1
jobs:
  build:
    machine:
      image: ubuntu-2204:current
      #docker_layer_caching: true
    resource_class: large
    steps:
      - checkout
      #- run: |
      #    docker login -u seungjin --password-stdin codeberg.org
      - run: |
          docker build --platform linux/amd64 --push -t codeberg.org/sa/mstd-random-restaurant:latest .





































--**-Mg: config.yml                       (fundamental-c)--L7--C33-------------------------------------------------
